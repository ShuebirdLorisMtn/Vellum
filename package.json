{
  "name": "vellum-backend",
  "version": "0.1.0",
  "description": "Minimal backend for Vellum: Claude integration + Whop webhook + one-free-document logic",
  "main": "src/server.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon --watch src --exec node src/server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "pg": "^8.10.0",
    "dotenv": "^16.0.3",
    "jsonwebtoken": "^9.0.0",
    "@sendgrid/mail": "^7.12.0",
    "node-fetch": "^3.4.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}
